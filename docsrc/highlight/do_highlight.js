// MATLAB code syntax highlighting via highlight.js

// Change css selector (highlight.js default is "pre code", which EasySpin doesn't use)
hljs.configure({cssSelector:"pre"});

// Limit langauge autodetection
hljs.configure({language:"matlab"});

// Applu syntax highlighting to all elements
hljs.highlightAll();
